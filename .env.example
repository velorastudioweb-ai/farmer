# Vite environment variables for HAiFarmer (prefix with VITE_)
# Copy this to .env and fill in values
# On Netlify, set these as Environment Variables in the deploy settings

# Backend API URL (e.g., https://haifarmer-api.onrender.com/api)
VITE_API_URL=http://localhost:5000/api
